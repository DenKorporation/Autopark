using Autofac;

namespace Autopark.Common;

public class CommonAutofacModule : AutofacModuleBase
{
    protected override void Load(ContainerBuilder builder)
    {
        base.Load(builder);
    }
}
