namespace Autopark.Common.Constants;

public static class Environments
{
    /// <summary>
    /// Specifies the Docker environment.
    /// </summary>
    public static readonly string Docker = nameof(Docker);

    /// <summary>
    /// Specifies the Testing environment.
    /// </summary>
    public static readonly string Testing = nameof(Testing);
}